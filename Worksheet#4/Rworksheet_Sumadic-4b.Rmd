---
title: "RWorksheet 4b"
author: "Andrey Sumadic"
date: "2023-11-08"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
#1.
vectorB <- c(1,2,3,4,5)

matrixB <- matrix(0, nrow = 5, ncol = 5) 
print(matrixB)
for(i in 1:5){
  for(j in 1:5){
    matrixB[i,j] <- abs(vectorB[i] - vectorB[j])
  }
}
print(matrixB)


```

```{r}
#2.
for(i in 1:5) {
  for(j in 1:i) {
    cat("*")
  }
  cat("\n")
}
```

```{r}
#3.
print("Enter a positive integer to start the Fibonacci sequence: ")
start <- as.integer(readline(prompt = ""))

#repeat {
#  print(start)
#  if(start > 500) break
#  next <- start + tail(fib, n=1)
#  start <- next
#}

```


```{r}
#4.
#a.
shoe <- read.csv("shoe data.csv")
shoes <- shoe[c(1:6),]
shoes


```

```{r}
#5.
expenses <- c(60, 10, 5, 25)
labels <- c("Food", "Electricity", "Savings", "Miscellaneous")

pie(expenses, labels = labels, col = c("green","blue","yellow","red"), 
    main = "Monthly Expenses of Dela Cruz Family")

```

```{r}
#6.
#a.
data(iris)
str(iris)
```

```{r}
#b.

```

```{r}
#c.
species <- table(iris$Species)
pie(species, labels = names(species), col = c("blue","orange","green"), main = "Species Distribution in Iris Data")
```

```{r}
#d.
setosa <- iris[iris$Species=="setosa",]
versicolor <- iris[iris$Species=="versicolor",]  
virginica <- iris[iris$Species=="virginica",]

tail(setosa, n=6)
tail(versicolor, n=6)
tail(virginica, n=6)
```

```{r}
#e.

```

```{r}
#f.

```













